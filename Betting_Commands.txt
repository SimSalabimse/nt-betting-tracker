# Betting Commands (Best Version - CSV Logging Active)

## Core Rules (Always Active)
- Follow `robust_betting_protocol_v2.md` and `nt-betting-skills.md` by the letter in full.
- **Short Notes Rule is mandatory** in all bet_log.csv work.
- Use `scripts/safe_bet_log_edit.py` for safe, validated updates to bet_log.csv (append-only or targeted settlement).
- All GitHub updates must follow the Successful Push Workflow: tree verify → get content + SHA → full clean content push with correct sha → re-verify.
- Complete all research, logging, and pushes before giving final output.

## Current Logging Method: CSV (Active)

We are currently using `bet_log.csv` + `current_bankroll.md` as the live system.
The new `nt_betting_system/` scripts are prepared for future use but CSV logging is active now.

## Best Primary Command (Recommended)

"Run full analysis on the attached/current odds file.

Use adaptive research mode:
- Many matches → strong filtering first, then targeted research.
- Few matches or single match → switch to deeper research mode.

Apply stupid loss filter, DNB preference on high-variance profiles, tiered staking, and explicit risk/reward.

Log all recommended bets into bet_log.csv with short notes using safe logic.
Update current_bankroll.md correctly (Equity rule).

I will place every bet you recommend.

Output only in clean standardized format with bets table."

## Other Useful Commands

**After Settlements (Strict Version):**
"Here are the settlement results: [paste details].

Trigger full post-settlement-learning-reviewer. Do a proper structured deep dive:
- Analyze what worked vs what failed (especially losses).
- Identify clear patterns or variance sources.
- Update edges in sport_edges_and_filters.md if meaningful patterns appear (additive only).

Update bet_log.csv with short, high-quality settlement notes.
Update current_bankroll.md correctly.

Before giving the final summary, confirm that bet_log.csv was actually updated (show proof via tree + re-read if needed).

Then give a clean summary with:
- Batch performance
- Key lessons / patterns found
- Any edge updates made
- Next actions"

**Stats / Performance Check:**
"Show current performance and bankroll status."

**Full System Review:**
"Run meta review. Update edges if needed."

## Notes
- Short Notes only in bet_log.csv (concise: Result + outcome vs prediction + 1 key lesson).
- Prefer `scripts/safe_bet_log_edit.py` for bet_log updates when possible.
- All GitHub pushes must follow the full Successful Push Workflow with verification.
- Post-settlement learning must be real and structured — not generic.