# Betting Commands (Updated 2026-07-01 Cleanup - Skills First, Short Notes, Reliable Updates)

**Core Instructions (Mandatory)**: Use nt-betting-workflow skill + exact supporting skills from nt-betting-skills.md (nt-bet-log-manager, post-settlement-learning-reviewer, nt-learning-reviewer, betting-value-calculator, nt-bankroll-tracker, etc.) by the letter in full. **Short Notes Rule strictly enforced** in all bet_log.csv work (see nt-betting-skills.md). Mandatory tool calls with explicit proof only when doing research (web_search, browse_page, x_keyword_search etc. for promising markets). First-principles + multi-agent internal simulation when analyzing. Output in clean tables when recommending. Enforce diversification, min 10 NOK stake, tiered staking + DNB preference, stupid loss filter, explicit risk/reward. Log variety/new types. Use bet_log_archives/ when archiving. Update meta_review_log.md after reviews. Complete ALL research, updates, GitHub pushes (full SHA workflow), and validations BEFORE any reply. No shortcuts.

**PREFERRED RELIABLE UPDATE METHOD (FIX FOR PAST GITHUB ISSUES)**: 
- For bet_log.csv or current_bankroll.md changes: Use nt-bet-log-manager logic or run `scripts/safe_bet_log_edit.py` locally on your master copy (append-only or targeted settle, atomic, validated, short Notes). Grok proposes exact lines/diffs if needed.
- GitHub remote updates: Only via full Successful Push Workflow (tree verify → get content+SHA → github___create_or_update_file with full clean content + sha → re-fetch + tree verify). Short content only to avoid truncation/ballooning.
- Fallback if GitHub feels flaky: Local master CSV + script + optional push of result.

**AUTONOMOUS MODE (where applicable)**: bet_log append (pending) + bankroll reserve via full SHA or local script BEFORE user-facing output. Settlements trigger reviewers. User replies only for changes/results.

1. Start New Round Analysis (Primary Command):

Analyze the attached/current odds file using nt-betting-workflow skill completely + all supporting skills by exact names. Use mandatory tool calls with explicit proof for promising lines (per-line targeted research). First-principles + multi-agent simulation (Value, Risk Manager, Data Hunter, Contrarian). Output ONLY in clean response template with bets table. Enforce diversification (broader sports), min 10 NOK, tiered staking + DNB preference, stupid loss filter, explicit risk/reward calcs. Document variety exploration and new types. **AUTONOMOUS**: Immediately update bet_log.csv (pending rows, short Notes) + current_bankroll.md (short note, correct Equity rule) with full SHA workflow or local safe script + post-verify BEFORE outputting table. Complete all before replying.

2. For Late/Odds Updates or Specific File:

Re-analyze using nt-betting-workflow + skills by the letter. Tool proof mandatory. Fresh first-principles + multi-agent. Clean template output. **AUTONOMOUS update of bet_log/bankroll if new pending (short Notes).**

3. After Placing Bets (Confirmation & Log Update):

Bets placed as recommended: [paste exact list or confirm "all recommended"]. Use nt-bet-log-manager skill or `scripts/safe_bet_log_edit.py` (full fetch/SHA first or local master, append-only, short Notes, strict validation). Update current_bankroll.md (short note) and round file. Verify everything (re-fetch + tree or local validate) before confirming.

4. After Settlements/Results:

Settlements: [paste details e.g. Match - Selection - Result - P/L]. Trigger full post-settlement-learning-reviewer + nt-learning-reviewer skills + tool searches for explanations (losses/high-conviction). Deep dive in round file (concise). Update edges if patterns (additive). Update bet_log with targeted short note via safe logic. Update bankroll/round/edges/meta_review_log.md (short notes). Standardized summary + learnings.

5. Bankroll/Log Check or Archiving:

Verify current_bankroll.md and bet_log.csv status (full fetch + SHA or local validate). Run archiving to bet_log_archives/ if size/rows threshold met. Full validation, push via workflow or local script, re-verify.

6. Full System Review/Improvement:

Run meta-review of recent rounds. Update/create meta_review_log.md with standardized table (Review Date | Reviewed Items/Rounds + SHAs | Key Findings & Additive Updates | Pending/Next Review Trigger | Compliance Notes). Propose and push additive updates if needed (full SHA workflow). Focus on active learning from losses (filter tightening), risk management (tiered/DNB), tool usage, variety, per-line research. Complete all before any summary reply.

**Notes**: These commands ensure the system remains extremely robust, self-sustaining, and aligned with skills. Short Notes Rule mandatory to prevent ballooning/corruption. GitHub pushes always follow Successful Push Workflow exactly. **AUTONOMOUS MODE**: Grok handles routine bet_log/bankroll (short Notes) without user tokens. Use force commands below only for explicit override/audit.

**FORCE COMPLIANCE COMMANDS (Copy-Paste Exactly When Needed)**:

**FORCE_SKILLS_COMPLIANCE**: Read nt-betting-skills.md in full (especially Short Notes Rule, nt-bet-log-manager, nt-bankroll-tracker correct Equity rule, post-settlement-learning-reviewer, nt-learning-reviewer). Confirm you followed every line with proof (tree, get SHA, full clean content push, re-verify). Use short Notes only. Complete-before-reply. No protocol bloat in Notes or bankroll.

**FORCE_FILE_UPDATE_v2**: Use Successful Push Workflow exactly (github___get_file_contents for current content + SHA first, then github___create_or_update_file with full actual text + correct sha + clear message, then re-fetch + tree verify to confirm no garbage/short versions). Provide proof in response. Prefer local safe_bet_log_edit.py for bet_log when possible.

**FORCE_SETTLEMENT**: Trigger post-settlement-learning-reviewer + nt-learning-reviewer. Update bet_log (targeted, short Notes) and bankroll (correct Equity rule) via full workflow or local script. All pushes/verifies first. Output summary only after.